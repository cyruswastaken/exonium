{
  "name": "exonium-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:client\" \"npm run dev:landing\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:client": "cd client && npm run dev", 
    "dev:landing": "cd landing && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../client && npm install && cd ../landing && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}